import Link from 'next/link';import { fundingProjects } from '@/data/dummy';
export default function My(){return <div><h1 className="text-2xl font-bold mb-3">Daftar Pengajuan Saya</h1>{fundingProjects.length===0?<div className="card">Belum ada pengajuan.</div>:<div className="grid gap-3">{fundingProjects.map(p=><div key={p.id} className="card"><span className="badge bg-emerald-100 text-emerald-700">{p.status}</span><h3 className="font-semibold">{p.title}</h3><p>Target: Rp{p.target_funding.toLocaleString('id-ID')}</p><Link href={`/funding/${p.id}`} className="text-sky-700">Detail</Link></div>)}</div>}</div>}
